let
    throttled = false,
    timeout = null,
    width,
    height,
    centre,
    fpsInterval,
    now,
    elapsed,
    then,
    startTime,
    play = true,
    darkMode = true,
    iteration = 0
;
