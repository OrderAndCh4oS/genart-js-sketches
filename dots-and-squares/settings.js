let
    throttled = false,
    timeout = null,
    width,
    height,
    centre,
    fpsInterval,
    now,
    elapsed,
    then,
    startTime,
    play = true,
    darkMode = false,
    iteration = 0
;
